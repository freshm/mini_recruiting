<%= form_for(@job_application, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @job_application.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@job_application.errors.count, "error") %> prohibited this job_application from being saved:</h4>

      <ul>
      <% @job_application.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :wage, :class => "col-lg-2 control-label" %>
    <div class="col-lg-10">
      <%= f.text_field :wage, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :note, :class => "col-lg-2 control-label" %>
    <div class="col-lg-10">
      <%= f.text_area :note, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :applicant_id, :class => "col-lg-2 control-label" %>
    <div class="col-lg-10">
      <%= f.number_field :applicant_id, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :advertisement_id, :class => "col-lg-2 control-label" %>
    <div class="col-lg-10">
      <%= f.number_field :advertisement_id, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
