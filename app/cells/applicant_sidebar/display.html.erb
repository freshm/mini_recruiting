<h2>Your Job Applications</h2>
<% if @applications.any? %>
  <table class="table">
    <% @applications.each do |application| %>
      <tr>
        <td><%= link_to application.vacancy.title, vacancy_path(application.vacancy) %></td>
      </tr>
    <% end %>
  </table>
<% end %>